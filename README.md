/
├── app/                
│   ├── layout.tsx      # Layout principal (se comparte entre páginas)
│   ├── page.tsx        # Página principal (ej. "/")
│   └── login/
│       └── page.tsx    # Página de login
├── public/             # Archivos estáticos (imágenes, favicon, etc.)
├── styles/             # Tailwind config
├── components/         # Componentes reutilizables (Navbar, Footer, etc.)
├── lib/                # Funciones auxiliares (ej. conexión DB, auth)
├── middleware.ts       
├── package.json        
└── next.config.js      
